---
title: "Feature mapping"
description: "Native feature coverage and parity mapping against backend/PWA behavior."
---

## Feature parity with backend/PWA

`d-sports-engage-native` targets high parity with `d-sports-api` feature behavior while optimizing for mobile UX patterns.

## Wallet and pack opening

- Wallet screens consume balances, token data, and secure key/pin flows from backend APIs.
- Pack browsing, purchase, and open flows use backend-driven states and inventory refresh behavior.
- Odds-disclosure surfaces read backend-provided probability payloads.

## Quest, rewards, and pass surfaces

- Quest UI supports team-aware progression and pass-gated eligibility.
- Reward states and claim flows follow backend quest/reward APIs.
- Global pass and event pass effects are reflected in native quest visibility/completion UX.

## Profile badge system

- A badge registry in `lib/badge-asset-map.ts` maps each achievement to display metadata (description, accent color, glow color, rarity tier).
- Supported badge asset keys: `rookie-fan`, `digital-badge`, `exclusive-digital-content`, `merchandise`, `rep-boost`, `mvp-starter`, `rep-vault`, `mvp-elite`.
- Badge artwork is served from Supabase public storage (`badge-assets` bucket). When the API response omits `badgeImageUrl`, the client resolves artwork via `getPublicBadgeImageUrl` using the achievement ID and label.
- On native platforms, resolved badge images are cached locally using MMKV and `expo-file-system` to avoid redundant downloads. On web, `expo-image` prefetch is used instead.
- A local fallback asset is bundled for the `exclusive-digital-content` badge for dev and offline use.

## Public profile navigation

- Viewing another user's profile at `/profile/[handle]` no longer activates the Profile tab pill, preventing incorrect tab highlighting when navigating from other tabs.

## Team and leaderboard integration

- Teams and leagues metadata are consumed from backend `/api/teams` and `/api/leagues`.
- Leaderboard displays and team-scoped ranking views align with backend leaderboard contracts.
- Favorite-team interactions influence quest and leaderboard context where backend eligibility requires it.
