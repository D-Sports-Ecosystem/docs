---
title: "d-sports-engage-native"
description: "Native iOS and Android app for D-Sports Engage. Expo 54, React Native, Clerk, RevenueCat, Thirdweb."
icon: "smartphone"
---

## Overview

**d-sports-engage-native** (package name: `engage-native`) is the native mobile app for D-Sports. It mirrors the core PWA experience on iOS and Android: wallet, shop, leaderboard, locker room, and profile.

- **Run:** `bunx expo start` or `bun run start` — then press `a` for Android or `i` for iOS, or scan the QR code with Expo Go.

## Tech stack

| Category       | Technology                                  |
| -------------- | ------------------------------------------- |
| Framework      | Expo 54, React Native 0.81, React 19        |
| Auth           | Clerk (Expo)                                |
| Payments       | RevenueCat (react-native-purchases)          |
| Web3           | Thirdweb                                    |
| State          | Zustand                                     |
| Storage        | MMKV                                        |
| UI             | Lucide React Native                         |
| Navigation     | Expo Router 6                               |
| Monitoring     | Sentry                                      |
| Push notifications | OneSignal                               |
| Package        | Bun                                         |

## Features

- **Wallet** — Tokens, holdings, pack opening, crypto checkout (via PWA backend)
- **Shop** — Collectibles, cart, coin bundles, checkout
- **Leaderboard** — Rankings and filters
- **Locker room** — Social feed, quests, and engagement
- **Quests & rewards** — Global, team-scoped, and event-scoped quests with a step-based progression and reward track (free and paid tiers)
- **Profile** — User profile, settings, and follow system
- **Theme** — Dark/light mode (default dark)
- **PWA support** — Responsive desktop layout with web hover states and `display: standalone`

## Getting started

<Steps>
  <Step title="Install dependencies">
    ```bash
    bun install
    ```
  </Step>
  <Step title="Configure environment variables">
    Create a `.env` file at the project root. Only `EXPO_PUBLIC_*` keys are accessible at runtime.

    | Variable | Purpose |
    | -------- | ------- |
    | `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` | Clerk authentication |
    | `EXPO_PUBLIC_API_URL` | Backend API base URL (e.g. `https://api.d-sports.org`) |
    | `EXPO_PUBLIC_TW_CLIENT_ID` | Thirdweb Web3 client ID |
    | `EXPO_PUBLIC_REVENUECAT_API_KEY` | RevenueCat API key |
    | `EXPO_PUBLIC_REVENUECAT_APPSTORE_ID` | RevenueCat App Store ID |
    | `EXPO_PUBLIC_REVENUECAT_ENTITLEMENT` | RevenueCat entitlement identifier |
    | `EXPO_PUBLIC_SUPABASE_URL` | Supabase project URL |
    | `EXPO_PUBLIC_SUPABASE_KEY` | Supabase publishable key |
  </Step>
  <Step title="Start the dev server">
    ```bash
    bunx expo start
    ```
    Press **a** for Android, **i** for iOS, or scan the QR code with Expo Go.
  </Step>
</Steps>

## EAS build profiles

The project uses EAS Build with these profiles defined in `eas.json`:

| Profile | Purpose | Notes |
| ------- | ------- | ----- |
| `development` | Dev client with simulator support | `bun run build:dev` |
| `development-device` | Dev client on physical device | Internal distribution |
| `preview` | Internal testing | Android APK, internal distribution |
| `staging` | Pre-production | Auto-incrementing version |
| `production` | App Store / Play Store release | Auto-incrementing version |

## Project structure

```
app/
├── (auth)/              # Login, signup, SSO callback
├── (onboarding)/        # New-user onboarding flow
├── (tabs)/              # Main tab navigation (wallet, shop, leaderboard, locker room, profile)
├── settings/            # Settings pages with nested modals and tabs
└── _layout.tsx          # Root layout with providers and auth protection

components/
├── wallet/              # Wallet sub-components (TokenRow, PackOpeningModal, etc.)
├── shop/                # Shop sub-components (CartModal, CryptoCheckoutModal, etc.)
├── locker-room/         # Locker room components including QuestSection
├── ui/                  # Reusable primitives (Button, TextField, WebHoverWrapper)
└── Icon/                # Icon wrapper using lucide-react-native

hooks/                   # Screen hooks (use-wallet-screen, use-shop-screen, etc.)
lib/api/                 # API client modules (wallet, shop, user, quests, checkout, etc.)
context/                 # React Context providers (user, collectibles, navbar visibility)
services/                # Zustand store, MMKV storage adapter, core types
theme/                   # Color system, spacing, typography tokens
types/                   # Shared TypeScript types (wallet, shop, checkout, API)
constants/               # Shared constants (wallet tokens, shop bundles, etc.)
```

## API client layer

The app communicates with the backend (`d-sports-api`) through an API client in `lib/api/`. Each domain has its own module:

| Module | Endpoints |
| ------ | --------- |
| `user-api` | Profile, authentication, follow/unfollow |
| `wallet-api` | Tokens, holdings, transactions |
| `shop-api` | Products, featured packs |
| `checkout-api` | Crypto checkout (`POST /api/checkout/crypto`) and verification |
| `quests-api` | Quests (global, team, event scoped), daily visit, step completion |
| `leaderboard-api` | Rankings and filters |
| `locker-room-api` | Social feed posts |
| `teams-api` | Team listings and membership |
| `collectibles-api` | Owned packs and items |
| `games-api` | Mini-games (pick-em, spin wheel, guess the player) |

All modules inject a Clerk auth token via `createApiClient` and fall back to MMKV cache when offline (`lib/api/cache.ts`).

<Card title="Ecosystem overview" icon="map" href="/repositories/ecosystem-overview">
  See how the native app fits with the PWA, site, and Mic'd Up.
</Card>
