---
title: "d-sports-engage-native"
description: "Native iOS and Android app for D-Sports Engage. Expo 54, React Native, Clerk, RevenueCat, Thirdweb."
icon: "smartphone"
---

## Overview

**d-sports-engage-native** (package name: `engage-native`, version `1.10.10`) is the native mobile app for D-Sports. It mirrors the core PWA experience on iOS and Android: wallet, shop, leaderboard, locker room, and profile. The app also runs as a responsive PWA on the web.

## Tech stack

| Category     | Technology                                |
| ------------ | ----------------------------------------- |
| Framework    | Expo 54, React Native 0.81, React 19     |
| Auth         | Clerk (`@clerk/clerk-expo`)               |
| Payments     | RevenueCat (`react-native-purchases`)     |
| Web3         | Thirdweb SDK                              |
| State        | Zustand 5                                 |
| Storage      | MMKV (`react-native-mmkv`)               |
| UI           | Lucide React Native                       |
| Navigation   | Expo Router 6 (file-based, typed routes)  |
| Animations   | React Native Reanimated 4                 |
| Error tracking | Sentry (`@sentry/react-native`)        |
| Package manager | Bun                                    |

## Features

- **Wallet** — tokens, holdings, pack opening, crypto checkout (via d-sports-api backend)
- **Shop** — collectibles, cart, coin bundles, fiat and crypto checkout
- **Leaderboard** — rankings and filters
- **Locker room** — social feed, quests, daily games (Pick'Em, Spin Wheel, Guess the Player)
- **Profile** — user profile, settings, team membership
- **Onboarding** — guided new-user onboarding flow
- **Theme** — dark/light mode (default dark)
- **PWA** — `display: standalone`, responsive desktop layout (max 480 px), web hover states

## Project structure

```text
app/
├── (auth)/              # Login, signup, SSO callback, password reset
├── (onboarding)/        # New-user onboarding flow
├── (tabs)/              # Main tab navigation (wallet, shop, leaderboard, locker room, profile)
├── settings/            # Settings pages with nested modals and tabs
├── profile/[handle].tsx # Public profile view
└── _layout.tsx          # Root layout with providers and auth protection

components/
├── wallet/              # Wallet sub-components (TabButton, TokenRow, PackOpeningModal, etc.)
├── shop/                # Shop sub-components (CartModal, CryptoCheckoutModal, etc.)
├── locker-room/         # Locker room components (quests, games, feed, teams)
├── leaderboard/         # Leaderboard components
├── settings/            # Settings tabs and modals
├── ui/                  # Reusable primitives (Button, TextField, WebHoverWrapper)
├── Icon/                # Icon wrapper (lucide-react-native)
└── theme-provider.tsx   # Theme context (dark/light)

hooks/
├── use-wallet-screen.ts # All wallet state, effects, and handlers
├── use-shop-screen.ts   # All shop state, effects, and handlers
├── use-feed-section.ts  # Social feed logic
└── use-carousel-scroll.ts # Carousel auto-scroll

lib/
├── api/                 # API client with Clerk auth-token injection and MMKV cache
├── revenuecat/          # RevenueCat in-app purchases provider
├── crypto/              # On-chain transaction signing (Thirdweb)
└── utils.ts             # Shared utilities

context/
├── user-context.tsx     # Auth, user profile, team membership
├── collectibles-context.tsx # Owned packs and items
├── navbar-visibility-context.tsx
└── create-action-context.tsx

services/
├── store.ts             # Zustand global store (theme, cart, points)
├── storage.ts           # MMKV persistence adapter
└── types.ts             # Core types (User, Room, Team, Product)
```

## Getting started

<Steps>
  <Step title="Install dependencies">
    ```bash
    bun install
    ```
  </Step>
  <Step title="Configure environment variables">
    Create a `.env` file at the project root. Only `EXPO_PUBLIC_*` keys are accessible at runtime.

    | Variable | Description |
    | -------- | ----------- |
    | `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` | Clerk publishable key for authentication |
    | `EXPO_PUBLIC_API_URL` | Backend API base URL (e.g. `https://api.d-sports.org`) |
    | `EXPO_PUBLIC_TW_CLIENT_ID` | Thirdweb client ID for Web3 |
    | `EXPO_PUBLIC_REVENUECAT_API_KEY` | RevenueCat API key |
    | `EXPO_PUBLIC_REVENUECAT_APPSTORE_ID` | RevenueCat App Store app ID |
    | `EXPO_PUBLIC_REVENUECAT_ENTITLEMENT` | RevenueCat entitlement identifier |
    | `EXPO_PUBLIC_SUPABASE_URL` | Supabase project URL |
    | `EXPO_PUBLIC_SUPABASE_KEY` | Supabase publishable (anon) key |
  </Step>
  <Step title="Start the development server">
    ```bash
    bunx expo start
    ```
    Press **a** for Android, **i** for iOS, or scan the QR code with Expo Go.
  </Step>
</Steps>

## EAS build profiles

Builds use [Expo Application Services (EAS)](https://expo.dev/eas). Available profiles:

| Profile | Command | Distribution | Notes |
| ------- | ------- | ------------ | ----- |
| Development | `bun run build:dev` | Internal | Dev client, iOS simulator support |
| Development (device) | `bun run build:dev:ios` / `build:dev:android` | Internal | Physical device builds |
| Preview | `bun run build:preview` | Internal | Android APK, internal testing |
| Production | `bun run build:prod` | Store | Auto-increment version, store submission |

Submit to stores with `bun run submit:ios` or `bun run submit:android`.

## Architecture patterns

- **Modular screen architecture** — screen files contain only JSX; all state, effects, and handlers live in dedicated hooks (`use-wallet-screen.ts`, `use-shop-screen.ts`)
- **Extracted sub-components** — wallet and shop screens decomposed into `components/wallet/` and `components/shop/` with barrel exports
- **API client layer** — `lib/api/client.ts` injects Clerk auth tokens and parses normalized `{ success, data, error, code }` response envelopes; domain modules cover wallet, shop, user, quests, leaderboard, locker room, teams, collectibles, and checkout
- **MMKV cache fallback** — `lib/api/cache.ts` provides `fetchWithCache()` for offline-first data
- **Crypto checkout** — Thirdweb SDK calls the d-sports-api backend (`POST /api/checkout/crypto`) for on-chain payments (Arbitrum, Ethereum, Polygon)
- **RevenueCat** — fiat payments via Apple IAP (native), Google Play (native), and Stripe (web)
- **Zustand + MMKV** — global state with synchronous persistence
- **React Context** — authentication (`UserContext`), collectibles, navbar visibility
- **Path alias** — `@/*` maps to project root

<Card title="Ecosystem overview" icon="map" href="/repositories/ecosystem-overview">
  See how the native app fits with the PWA, site, and Mic'd Up.
</Card>
