---
title: "D-Sports Ecosystem Overview"
description: "Overview of the D-Sports repositories and how they fit together"
---

## Repository topology

| Repository | Purpose | Runtime |
| --- | --- | --- |
| **d-sports-api** | Primary backend + PWA + API surface | `bun dev` (3004) |
| **d-sports-engage-native** | Native iOS/Android app using backend APIs | `bunx expo start` |
| **d-sports-site** | Marketing/public web site | `bun dev` (3005) |
| **d-sports-mic-d-up** | Live audio product with separate backend | separate backend/mobile |
| **leagues** | Canonical league/team branding and metadata source | data + sync workflow |

## Cross-repo data flow

`d-sports-api` is the runtime API center for native and web clients. `leagues` acts as canonical metadata input for teams/leagues that is synchronized into backend storage and then surfaced via API reads.

## Teams and leagues pipeline

1. `leagues` repository updates canonical assets/data.
2. Workflow trigger sends webhook to backend sync route.
3. Backend sync services upsert leagues, team logos, and links.
4. Clients consume updates through `/api/teams` and `/api/leagues`.

## Quest and pass architecture across backend + native

- Backend gamification supports **per-team quests** and pass-aware eligibility.
- Pass gating includes **global pass** and **event pass** semantics.
- Reward flows include explicit handling for **free rewards** and **unpaid rewards** states.
- Native clients render eligibility/progression from backend response truth.

## Public vs internal documentation boundaries

- Public docs include contributor-focused implementation contracts.
- Sensitive admin internals, debug/dev routes, and cron/ops internals are excluded.
- Approved admin/team routes are documented with redaction where needed for contributor onboarding.
