---
title: 'RevenueCat webhook'
description: 'Handles RevenueCat purchase events (dsports-cash, Frontline Pass).'
openapi: 'POST /api/webhooks/revenuecat'
---

RevenueCat sends purchase events to this webhook. The request must include an `Authorization: Bearer <REVENUECAT_WEBHOOK_SECRET>` header.
